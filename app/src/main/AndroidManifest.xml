<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.octopusstudios.carnospace.lemp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/lemp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MissionsListerActivity"
            android:label="@string/missionList"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity
            android:name=".activity.MissionDetailsActivity"
            android:label="@string/missionDetails"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity
            android:name=".launcher.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>